[project]
name = "ytscrape-s"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "vinayak20injamure@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "faster-whisper>=1.2.1",
    "yt-dlp>=2025.1.0",
]

[project.scripts]
ytscrape-s = "ytscrape_s:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
